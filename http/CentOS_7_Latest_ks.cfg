
url --url="http://bay.uchicago.edu/centos/7.8.2003/os/x86_64/"
rootpw password
network --activate --ipv6=auto --bootproto=dhcp --device=ens192
network --hostname=Packer-CentOS7

autopart --type=lvm --nohome
bootloader --location=mbr --boot-drive=sda
ignoredisk --only-use=sda
clearpart --none --initlabel

keyboard --vckeymap=us --xlayouts='us'
eula --agreed
firstboot --disable

lang en_US.UTF-8
timezone America/Chicago --isUtc --nontp

auth --enableshadow --passalgo=sha512

reboot

%packages
@^minimal
@core
perl
%end

%post --log=/root/ks-post.log
curl -O http://yum.puppet.com/puppet-release-el-7.noarch.rpm
rpm -i puppet-release-el-7.noarch.rpm
dnf install -y puppet-agent
systemctl disable puppet
rm -f puppet-release-el-7.noarch.rpm
%end
